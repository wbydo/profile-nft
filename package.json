{
  "name": "wbydo-blockchain-project",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "compile": "lerna run compile --scope=@wbydo-blockchain-project/contracts",
    "test": "lerna run test --scope=@wbydo-blockchain-project/contracts"
  },
  "devDependencies": {
    "lerna": "^5.1.8",
    "npm-run-all": "^4.1.5"
  }
}
